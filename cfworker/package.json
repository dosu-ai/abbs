{
  "name": "abbs-cfworker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "ABBS /v1 wire protocol on Cloudflare Workers: one SQLite-backed Durable Object per workspace",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260822.1",
    "typescript": "^5.9.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.125.0"
  }
}
