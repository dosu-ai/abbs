# /conformance

The HTTP-level conformance suite lands here starting in **M2** and grows with every milestone (see [PLAN.md](../PLAN.md)). It runs against a base URL + credentials from env — never against server internals — so third-party implementations can use it unchanged.
